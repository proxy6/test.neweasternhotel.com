<%- include('../partials/header') %>
<%- include('../partials/sidenav') %>



            <div class="page-body">
                <div class="container-fluid">
    <div class="row page-title">
        <div class="col-sm-6">
            <h3>Complaint Management</h3>
        </div>
        <div class="col-sm-6">
            <nav>
                <ol class="breadcrumb justify-content-sm-end align-items-center">
                    <li class="breadcrumb-item"> <a href="/">
                            <svg class="svg-color">
                                <use href="/icons/iconly-sprite.svg#Home"></use>
                            </svg></a></li>
                    <li class="breadcrumb-item"><a href="/complaints" style="color: #43b9b299;">Complaint Management</a></li>
                    <li class="breadcrumb-item active">Add Complaint</li>
                </ol>
            </nav>
        </div>
    </div>
</div>
<div class="container-fluid">
    <div class="row">
        <div class="col-sm-12">
            <div class="card height-equal">
                <div class="card-header">
                    <h4>Add Complaint</h4>
                </div>
                <div class="card-body">
                    <form class="row g-3 custom-input" id="tagForm" action="/complaints/" method="POST">
                        <input type="hidden" name="_token" value="GNF8HVQ1jLKbedjPaOj3SsGWKwhykKifaaxEpzUp" autocomplete="off">
                        
                        <div class="col-6">
                            <label class="form-label" for="">Title <span> *</span></label>
                            <input class="form-control" type="text" value="" placeholder="Title" name="title" required>
                        </div>
                        
                        <div class="col-6">
                            <label class="form-label" for="">Room Number/Name</label>
                            
                            <select name="room_number" id="roomSelector" class="form-select">
                                <option value="">Select..</option>
                                <% if (typeof rooms !== 'undefined') { %>
                                    <% rooms.forEach(room => { %>
                                        <option data-room-id="<%= room.id %>" value="<%= room.number %>">
                                            <%= room.number.charAt(0).toUpperCase() + room.number.slice(1) %>
                                        </option>
                                    <% }); %>
                                <% } %>
                            </select>
                            <!-- Hidden input for room_id -->
                            <input type="hidden" id="roomIdInput" name="room_id" value="">
                        </div>
                    
                        <div class="col-12">
                            <label class="form-label" for="">Message <span> *</span></label>
                            <textarea name="message" rows="7" class="form-control" required></textarea>
                        </div>
                    
                        <div class="mt-3 text-end">
                            <button type="submit" class="btn btn-primary">Save</button>
                        </div>
                    </form>
                    
                </div>
            </div>
        </div>
    </div>
</div>

            </div>
                     
               <!-- footer start-->
               <%- include('../partials/footer') %>


            </div>
          </main>
                 
                  
                  
          <%- include('../partials/script') %>
          <script src="/js/custom/complaint.js"></script>


    

</body>
</html>
